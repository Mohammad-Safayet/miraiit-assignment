import 'package:flutter/material.dart';

abstract class AppColors {
  static const white = Color.fromRGBO(255, 255, 255, 0);
  static const black = Color.fromRGBO(0, 0, 0, 1);

  static const background = Color.fromRGBO(255, 255, 255, 1);
  static const onBackground = Colors.black54;

  static const primary = Color.fromRGBO(55, 173, 87, 1);

  static const searchBarColor = Color.fromRGBO(0, 0, 0, 0.05);
  static const cardBackgroundColor = Color.fromRGBO(245, 245, 245, 1);
}
